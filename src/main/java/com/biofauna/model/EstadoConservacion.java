package com.biofauna.model;

public enum EstadoConservacion {
    PREOCUPACION_MENOR,    // Least Concern (LC)
    CASI_AMENAZADO,        // Near Threatened (NT)
    VULNERABLE,            // Vulnerable (VU)
    EN_PELIGRO,            // Endangered (EN)
    EN_PELIGRO_CRITICO,    // Critically Endangered (CR)
    EXTINTO_EN_SILVESTRE,  // Extinct in the Wild (EW)
    EXTINTO                // Extinct (EX)
}
